# Generated automatically by org-governance-bot. Do not edit manually.
name: "Build Flutter APK"

on:
  push:
    branches:
      - main

jobs:
  orchestrate:
    uses: blinklabs-io/actions/.github/workflows/reuseable-flutter.yml@main
    secrets:
      GOOGLE_SERVICES_JSON: ${{ secrets.GOOGLE_SERVICES_JSON }}
    with:
      apk-artifact-name: app-${{ github.ref_name }}-release.apk
      build-apk: true
      channel: stable
      create-google-services: true
      flutter-version: 3.22.0
      run-analyze: false
      run-tests: false
